import React from 'react';
import QuickPropertyVerifier from '@/components/admin/QuickPropertyVerifier';

const QuickVerify: React.FC = () => {
  return <QuickPropertyVerifier />;
};

export default QuickVerify;
